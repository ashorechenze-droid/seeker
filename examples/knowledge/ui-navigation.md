# Desktop navigation behavior

The left sidebar can be collapsed to leave more room for the editor. Its toggle button preserves the previous width, and keyboard focus moves into the document after the panel closes.

Keep navigation items accessible: every action needs a readable label, visible focus state, and predictable tab order.
